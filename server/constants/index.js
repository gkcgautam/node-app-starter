const audio = require('./audio');
const image = require('./image');
const time = require('./time');
const video = require('./video');

module.exports = {
    audio,
    image,
    time,
    video,
};
