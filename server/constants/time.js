const DURATION_IN_MS = {
    SECOND: 1000,
    MINUTE: 60 * 1000,
    HOUR: 60 * 60 * 1000,
};

const DURATION_IN_SECS = {
    MINUTE: 60,
    HOUR: 60 * 60,
    DAY: 24 * 60 * 60,
};

module.exports = {
    DURATION_IN_MS,
    DURATION_IN_SECS,
};
